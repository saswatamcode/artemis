apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- namespace.yaml
- artemis-statefulset.yaml
- grafana-datasource-configmap.yaml

namespace: tracing

commonLabels:
  app.kubernetes.io/part-of: artemis-tracing

# Optionally customize the image tag
# images:
# - name: artemis
#   newTag: v1.0.0

# Optionally add resource limits
# patchesStrategicMerge:
# - |-
#   apiVersion: apps/v1
#   kind: StatefulSet
#   metadata:
#     name: artemis
#   spec:
#     template:
#       spec:
#         containers:
#         - name: artemis
#           resources:
#             requests:
#               memory: "1Gi"
#               cpu: "500m"
